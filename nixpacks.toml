[phases.setup]
nixPkgs = ["nodejs_18", "pnpm"]

[phases.install]
cmds = ["cd apps/api && pnpm install"]

[phases.build]
cmds = ["cd apps/api && pnpm prisma generate && pnpm build"]

[start]
cmd = "cd apps/api && node dist/main.js"
